# DNS Control — Environment Variables
# Copy this file to .env and adjust values for your environment.

# Backend API URL (required for production; omit for preview/mock mode)
VITE_API_URL=http://localhost:8000
