# DNS Control — Static data assets bundled with the backend generators.
# Files in this package (e.g. named.cache) are versioned snapshots used to
# guarantee deterministic, offline-reproducible deploys. NEVER fetch these
# from the network at runtime — update them via explicit PRs only.
